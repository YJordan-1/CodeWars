/*
DESCRIPTION
Simple, remove the spaces from the string, then return the resultant string.
*/

// ANSWER

function noSpace(x) {
  // split the string into an array of words
  let split = x.split(" ");

  // join the array of words into a single string
  let newWord = split.join("");
  return newString;
}
